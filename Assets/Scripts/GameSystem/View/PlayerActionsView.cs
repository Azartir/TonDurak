using UnityEngine;

namespace GameSystem.View
{
    public class PlayerActionsView : MonoBehaviour
    {
        //[SerializeField] private EndMoveButtonHandler _endMoveButton;
        //[SerializeField] private TakeButtonHandler _takeButtonHandler;

        //public void Init(PlayerHandler playerHandler)
        //{
        //    _endMoveButton.Init(playerHandler);
        //    _takeButtonHandler.Init(playerHandler);
        //}
    }
}